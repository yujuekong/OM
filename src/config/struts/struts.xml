<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 2.1//EN" "http://struts.apache.org/dtds/struts-2.1.dtd">
<struts>
	<!-- 开启使用开发模式，详细错误提示 -->
    <constant name="struts.devMode" value="false" />
    <!-- 将对象交给spring管理 -->
    <constant name="struts.objectFactory" value="spring" />
    <!-- 指定资源编码类型 -->
    <constant name="struts.i18n.encoding" value="UTF-8" /> 
	<!-- 指定每次请求到达，重新加载资源文件 -->
    <constant name="struts.i18n.reload" value="false" />
    <!-- 指定每次配置文件更改后，自动重新加载 -->
    <constant name="struts.configuration.xml.reload" value="false" />
    <!-- 国际化资源文件 -->
    <constant name="struts.custom.i18n.resources" value="resources/content/Language" />
    <!-- 默认后缀名 -->
    <constant name="struts.action.extension" value="do,action,jhtml,txt,png,jpg,docx,doc" />
    <!-- 系统默认上传文件最大上限 10M-->
    <constant name="struts.multipart.maxSize" value="100000000" />
    
    <constant name="struts.convention.package.locators" value="web,action,test" />
    
    <!--广告上传文件下载配置  -->
    <package name="default" namespace="/view/advert/advertInfo" extends="struts-default">
    	<action name="downLoadFile" class="com.smart.om.web.advert.AdvertInfoAction" method="downLoadFile">
    	 <result type="stream">  
           <param name="inputName">inputStream</param>  
           <param name="contentDisposition">attachment;filename="${adInfoUploadFileName}"</param>  
          </result>   
    	</action>
    </package>
</struts>    
